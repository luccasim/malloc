#include <stdlib.h>
#include "ft_printf.h"
#include "malloc.h"
#include "libft.h"
#include "ft_random.h"
#include <unistd.h>
#include <time.h>

int main(void)
{
	char *s;

	s = malloc(42);
	for(int i = 0; i < 1024; i++)
		s[i] = i;
	show_alloc_mem_ex(MALLOC_DUMP);
	return (0);
}